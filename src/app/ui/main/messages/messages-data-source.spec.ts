import { MessagesDataSource } from './messages-data-source';

describe('MessagesDataSource', () => {
  it('should create an instance', () => {
    expect(new MessagesDataSource()).toBeTruthy();
  });
});
